{
  "name": "w3-paypal-action",
  "version": "0.1.0",
  "private": true,
  "description": "PayPal payments, orders, payouts, subscriptions, invoicing, disputes, and vault for W3 workflows",
  "main": "src/index.js",
  "scripts": {
    "build": "npx @vercel/ncc build src/index.js -o dist"
  },
  "dependencies": {
    "@actions/core": "^1.11.1"
  }
}
